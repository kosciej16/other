[project]
name = "app"
version = "0.0.1"
dependencies = ["xdg"]

# [build-system]
# requires = ["hatchling"]
# build-backend = "hatchling.build"
#
# [tool.hatch.build.targets.wheel]
# packages = ["src/commons"]
